<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="com.ddosirak.mapper.OrderMapper">
  
  	<!-- 발주서 등록 -->
  	<insert id="insertOrder">
  		INSERT INTO ret_order 
  		(order_id,order_number, material_code, material_name, material_price, order_piece, order_date, pay_date, order_resp, order_trade)
		VALUES (null,CONCAT('ORD_',replace(#{order_date},'-','')),#{material_code},#{material_name},#{material_price},#{order_piece},#{order_date},#{pay_date},#{order_resp},#{order_trade})
  	</insert>
  	<!-- 발주서 등록 -->
  	
  	<!-- 발주현황 리스트 -->
  	<select id="orderAllList" resultType="com.ddosirak.domain.OrderVO">
  		SELECT * FROM ret_order 
  	</select>
  	<!-- 발주현황 리스트 -->
  	
  </mapper>