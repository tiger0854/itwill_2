<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
 <mapper namespace="com.ddosirak.mapper.pageMapper">
  	<!-- 게시판 페이징 처리를 위한 쿼리 -->
	<select id="countBoard" resultType="Integer">
		SELECT COUNT(*) FROM emp_board
	</select>
	<!-- 게시판 페이징 처리를 위한 쿼리 -->
	
	<!-- 사원목록 페이징 처리를 위한 쿼리 -->
	<select id="countEmpList" resultType="Integer">
		SELECT COUNT(*) FROM emp_main
	</select>
	<!-- 사원목록 페이징 처리를 위한 쿼리 -->
	
	<!-- 거래처목록 페이징 처리를 위한 쿼리 -->
	<select id="customerList" resultType="Integer">
		SELECT COUNT(*) FROM ret_customer
	</select>
	<!-- 거래처목록 페이징 처리를 위한 쿼리 -->
	
	<!-- 거래처목록 페이징 처리를 위한 쿼리 -->
	<select id="serchCusname"  parameterType="com.ddosirak.domain.PageVO" resultType="Integer">
		SELECT COUNT(*) FROM ret_customer
		WHERE 
			<choose>
  				<when test="kind == 'name'">cus_name</when>
  				<when test="kind == 'code'">cus_code</when>
  			</choose> 
		LIKE CONCAT('%', #{search}, '%')
	</select>
	<!-- 거래처목록 페이징 처리를 위한 쿼리 -->
	
  
  </mapper>